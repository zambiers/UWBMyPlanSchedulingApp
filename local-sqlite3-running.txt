load in from .db file

outside sqlite3 shell:
sqlite3 existing.db
sqlite3 existing.db "SELECT COUNT(*) FROM table1;"

inside sqlite3 shell:
.open existing.db

manually load in all data from inside sqlite3 shell:
.mode box
.headers on
.read courseScheduler.session.sql
.read dataInsertion.sql
.tables (currently 5 tables, we need at least one for for the Student to Course index)
SELECT COUNT(*) FROM Courses;
SELECT CourseCode, CourseName FROM Courses LIMIT 5;
SELECT * FROM Section LIMIT 5;
SELECT * FROM Professors LIMIT 5;

To save .db file when already in sqlite3
.save newname.db
  
If just in terminal (not sqlite3)
sqlite3 newname.db ".read courseScheduler.session.sql" ".read dataInsertion.sql"
